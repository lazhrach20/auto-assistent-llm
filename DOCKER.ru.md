# Руководство по развертыванию Docker

Это руководство показывает, как развернуть монорепозиторий Auto Assistent с использованием Docker.

[🇬🇧 English version](DOCKER.md)

## Требования

- Docker 20.10+
- Docker Compose 2.0+
- Скопируйте `.env.example` в `.env` и заполните свои учетные данные

## Быстрый старт

### 1. Подготовка окружения

```bash
# Скопировать файл окружения
cp .env.example .env

# Отредактировать .env со своими учетными данными (используйте текстовый редактор)
# Минимально необходимые:
# - POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
# - JWT_SECRET
# - ADMIN_PASSWORD
```

### 2. Сборка и запуск всех сервисов

```bash
docker-compose up -d
```

Это выполнит:
- Запуск базы данных PostgreSQL
- **Автоматический запуск миграций Alembic** ✨
- **Автоматическое создание admin пользователя** ✨
- Запуск Backend API (порт 8000)
- Запуск Web Scraper (фоновый воркер)
- Запуск Telegram Bot (фоновый воркер)
- Запуск Frontend (порт 5173)

### 3. Проверка сервисов

```bash
# Проверить, что все сервисы запущены
docker-compose ps

# Просмотр логов backend (миграции + seeding)
docker-compose logs backend

# Должно быть видно:
#   ✅ PostgreSQL is ready!
#   🔄 Running Alembic migrations...
#   🌱 Seeding database...
#   🚀 Starting application...
```

### 4. Доступ к приложению

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **Логин**: имя пользователя `admin`, пароль из `.env` (по умолчанию: `admin123`)

## Ручные миграции не нужны! 🎉

Backend сервис теперь включает **автоматический скрипт инициализации** (`entrypoint.sh`), который:
1. ✅ Ожидает готовности PostgreSQL
2. ✅ Автоматически запускает `alembic upgrade head`
3. ✅ Создает admin пользователя (идемпотентно - не дублирует)
4. ✅ Запускает приложение

Вам больше не нужно запускать миграции вручную!

## Основные команды

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис (полезно для отладки)
docker-compose logs -f backend
docker-compose logs -f scraper
docker-compose logs -f bot
docker-compose logs -f frontend
docker-compose logs -f db
```

### Перезапуск сервисов

```bash
# Все сервисы
docker-compose restart

# Конкретный сервис
docker-compose restart backend
```

### Остановка сервисов

```bash
# Остановить все (контейнеры остаются)
docker-compose stop

# Остановить и удалить контейнеры
docker-compose down

# Остановить и удалить все включая volumes (⚠️ удаляет базу данных!)
docker-compose down -v
```

### Пересборка образов

```bash
# Пересобрать все образы
docker-compose build

# Пересобрать конкретный сервис
docker-compose build backend

# Пересобрать и перезапустить (принудительные новые миграции/seeding)
docker-compose up -d --build
```

### Ручные миграции (если необходимо)

```bash
# Обычно не требуется - миграции выполняются автоматически
# Но вы можете запустить их вручную:
docker-compose exec backend alembic upgrade head

# Или создать новую миграцию
docker-compose exec backend alembic revision --autogenerate -m "описание"
```

### Повторный seeding базы данных

```bash
# Скрипт seeding идемпотентен - он не дублирует admin пользователя
docker-compose exec backend python seed.py
```

### Масштабирование сервисов

```bash
# Запустить несколько экземпляров scraper
docker-compose up -d --scale scraper=3
```

## Архитектура

```
┌─────────────┐     ┌──────────────┐
│   Frontend  │────▶│   Nginx      │
│  (React)    │     │   :80        │
└─────────────┘     └──────────────┘
                            │
                            ▼
                    ┌──────────────┐
                    │   Backend    │
                    │   FastAPI    │
                    │   :8000      │
                    └──────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Scraper    │    │   Telegram   │    │  PostgreSQL  │
│   Worker     │    │     Bot      │    │     :5432    │
└──────────────┘    └──────────────┘    └──────────────┘
```

## Размеры образов (реальные)

- **Backend**: 232 MB (multi-stage сборка)
- **Frontend**: 63 MB (Nginx + статические файлы)
- **Bot**: 229 MB (включает OpenAI клиент)
- **Scraper**: 194 MB (httpx + BeautifulSoup4)
- **PostgreSQL**: 274 MB (Официальный `postgres:15-alpine`)

**Итого: ~992 MB** (менее 1GB для всего стека)

## Функции безопасности

✅ Multi-stage сборки (маленькие финальные образы)
✅ Non-root пользователи для всех сервисов
✅ Read-only volumes где возможно
✅ Health checks для всех сервисов
✅ Изолированная bridge сеть
✅ Нет инструментов сборки в production образах
✅ Заголовки безопасности в Nginx

## Соображения для production

### 1. Переменные окружения

Обновите `.env` с production значениями:
- Используйте сильный `JWT_SECRET` (сгенерируйте с помощью: `python -c "import secrets; print(secrets.token_urlsafe(32))"`)
- Используйте сильный пароль базы данных
- Замените `localhost` в `DATABASE_URL` на имя сервиса `db`

### 2. SSL/HTTPS

Добавьте reverse proxy (например, Traefik, Nginx Proxy Manager) для SSL терминации:

```yaml
# Пример: Добавить в docker-compose.yml
  nginx-proxy:
    image: jwilder/nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./certs:/etc/nginx/certs
```

### 3. Мониторинг

Добавьте сервисы мониторинга:

```yaml
  prometheus:
    image: prom/prometheus
    # ... конфигурация

  grafana:
    image: grafana/grafana
    # ... конфигурация
```

### 4. Резервное копирование базы данных

```bash
# Резервное копирование
docker-compose exec db pg_dump -U admin million_miles > backup.sql

# Восстановление
docker-compose exec -T db psql -U admin million_miles < backup.sql
```

## Устранение неполадок

### Сервис не запускается

```bash
# Проверить логи
docker-compose logs backend

# Проверить здоровье
docker-compose ps
```

### Ошибки подключения к базе данных

```bash
# Убедиться что БД здорова
docker-compose ps db

# Проверить подключение
docker-compose exec backend python -c "from database import engine; print('OK')"
```

### Frontend не может достучаться до backend

- Проверьте `VITE_API_URL` в окружении frontend
- Убедитесь что backend запущен: `curl http://localhost:8000/`

### Порт уже используется

```bash
# Найти процесс использующий порт
netstat -ano | findstr :8000  # Windows
lsof -i :8000                  # Linux/Mac

# Изменить порт в docker-compose.yml
ports:
  - "8001:8000"  # хост:контейнер
```

## Development vs Production

### Development (с hot reload)

```yaml
# Добавить к backend сервису
volumes:
  - ./backend:/app
command: uvicorn main:app --reload --host 0.0.0.0
```

### Production (оптимизировано)

Используйте предоставленные Dockerfile как есть для оптимизированных production образов.
