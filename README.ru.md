# 🚗 Auto Assistent - Система управления автомобильным инвентарем

[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?logo=docker&logoColor=white)](https://www.docker.com/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-009688?logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com/)
[![React](https://img.shields.io/badge/React-19-61DAFB?logo=react&logoColor=black)](https://react.dev/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-316192?logo=postgresql&logoColor=white)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

Production-ready полнофункциональная система управления автомобильным инвентарем с автоматическим парсингом, интеллектуальным поиском и адаптивной админ-панелью.

[🇬🇧 English version](README.md)

---

## 📋 Обзор проекта

**Auto Assistent** - это комплексная платформа управления автомобильным инвентарем, которая автоматизирует сбор, хранение и отображение объявлений о продаже автомобилей с японских автомобильных площадок. Система построена на современной микросервисной архитектуре:

```
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│   Scraper   │────▶│  PostgreSQL  │◀────│   Backend    │
│  (Worker)   │     │   Database   │     │   (FastAPI)  │
└─────────────┘     └──────────────┘     └──────────────┘
                            │                     │
                            ▼                     ▼
                    ┌──────────────┐     ┌──────────────┐
                    │  Telegram    │     │   Frontend   │
                    │     Bot      │     │   (React)    │
                    └──────────────┘     └──────────────┘
```

### Поток данных

1. **Scraper** извлекает объявления с carsensor.net и сохраняет в PostgreSQL
2. **Backend API** предоставляет RESTful endpoints с JWT аутентификацией
3. **Frontend Dashboard** отображает объявления с бесконечной прокруткой и серверным поиском
4. **Telegram Bot** позволяет делать запросы на естественном языке с помощью OpenAI GPT

---

## 🚀 Быстрый старт

### Требования

- **Docker** 20.10+ ([Установка](https://docs.docker.com/get-docker/))
- **Docker Compose** 2.0+ ([Установка](https://docs.docker.com/compose/install/))

### Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/auto-assistent-llm.git
cd auto-assistent-llm

# 2. Настроить переменные окружения
cp .env.example .env
# Отредактировать .env со своими данными:
#   - Установить POSTGRES_PASSWORD (сильный пароль)
#   - Установить JWT_SECRET (сгенерировать: python -c "import secrets; print(secrets.token_urlsafe(32))")
#   - Установить TG_BOT_TOKEN (от @BotFather - см. раздел ниже)
#   - Установить AI_API_KEY (от OpenAI)

# 3. Запустить все сервисы (миграции и seeding запускаются автоматически)
docker-compose up -d

# 4. Проверить, что сервисы работают
docker-compose ps
```

### Точки доступа

| Сервис | URL | Учетные данные |
|--------|-----|----------------|
| **Frontend Dashboard** | http://localhost:5173 | `admin` / `admin123` |
| **Backend API** | http://localhost:8000 | Требуется JWT |
| **API Документация** | http://localhost:8000/docs | Интерактивный Swagger UI |
| **Telegram Bot** | Username вашего бота | Запросы на естественном языке |

### Учетные данные администратора по умолчанию

```
Логин: admin
Пароль: admin123 (измените в .env перед деплоем)
```

### Настройка Telegram бота (опционально)

Если вы хотите использовать функцию Telegram бота, необходимо создать бота через BotFather:

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Начните чат и отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите **имя** для вашего бота (например, "Auto Assistent")
   - Введите **username** для вашего бота (должен заканчиваться на "bot", например, "auto_assistent_llm_bot")
4. BotFather ответит с токеном вашего бота. Он выглядит так:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
   ```
5. Скопируйте этот токен и вставьте его в переменную `TG_BOT_TOKEN` в вашем файле `.env`
6. (Опционально) Настройте вашего бота:
   - `/setdescription` - Установить описание бота
   - `/setabouttext` - Установить текст "О боте"
   - `/setuserpic` - Установить аватар бота

**Примечание:** Храните токен бота в безопасности и никогда не коммитьте его в систему контроля версий!

---

## 🎯 Архитектурные решения

### 1. Стратегия парсинга: httpx + BeautifulSoup

Используем `httpx` с `BeautifulSoup4` вместо браузерной автоматизации (Playwright/Selenium) потому что:
- **carsensor.net отдает статический HTML** - рендеринг JavaScript не требуется
- **В 10 раз меньше ресурсов** - ~50MB RAM против ~500MB для экземпляра браузера
- **Быстрее выполнение** - 50-200мс на запрос против 2-5 секунд с запуском браузера
- **Меньше Docker образы** - ~150MB против ~1.5GB с Chromium
- **Проще масштабирование** - stateless воркеры легче масштабировать горизонтально

### 2. Cursor-Based пагинация

Используем cursor-based пагинацию (`WHERE id < cursor`) вместо offset/limit потому что:
- **Постоянная производительность** - O(1) переходы по индексу независимо от глубины страницы
- **Без пропуска данных** - курсор сохраняет позицию при добавлении новых элементов
- **Без дубликатов** - предотвращает повторный показ элемента при конкурентных операциях
- **Лучше для бесконечной прокрутки** - предсказуемо низкая задержка на больших датасетах

### 3. Frontend стек: Ant Design + TanStack Query

**Ant Design** предоставляет:
- Профессиональные enterprise UI компоненты из коробки
- Встроенные адаптивные паттерны (Table для десктопа, List для мобильных)
- 100+ готовых компонентов, ускоряющих разработку

**TanStack Query** обрабатывает:
- Автоматическое кеширование и дедупликацию запросов
- Фоновую перезагрузку данных и optimistic updates
- Декларативную загрузку данных вместо ручного управления состоянием

---

## 🐳 Docker Архитектура

### Образы контейнеров

Все сервисы контейнеризированы с использованием **multi-stage сборок** и базовых образов **Alpine Linux** для оптимального размера:

| Сервис | Размер образа | Тип сборки |
|--------|---------------|------------|
| **Backend** | 232 MB | Multi-stage Python (slim → alpine) |
| **Frontend** | 63 MB | Multi-stage Node → Nginx |
| **Bot** | 229 MB | Python slim (включает OpenAI SDK) |
| **Scraper** | 194 MB | Python slim (httpx + BS4) |
| **PostgreSQL** | 274 MB | Официальный `postgres:15-alpine` |

**Общий размер стека: ~992 MB** (менее 1GB для всего приложения)

### Почему образы небольшие

✅ **Multi-stage сборки** - Зависимости для сборки отбрасываются, остаются только runtime артефакты  
✅ **Alpine Linux** - Минимальные базовые образы (~5MB против ~100MB для Ubuntu)  
✅ **Без инструментов сборки** - `gcc`, `make`, `npm` исключены из финальных образов  
✅ **Кеширование слоев** - Docker переиспользует неизмененные слои между сборками  
✅ **Оптимизированные зависимости** - Включены только production пакеты

### Как найти Docker образы

```bash
# Список всех образов проекта
docker images | grep auto-assistent-llm

# Список всех образов включая базовые
docker images

# Проверить образ PostgreSQL (официальный, без префикса проекта)
docker images postgres
```

**Примечание:** Образ PostgreSQL (`postgres:15-alpine`) - это официальный образ из Docker Hub, он не собирается в этом проекте. Поэтому отображается без префикса `auto-assistent-llm`.

---

## ✨ Возможности

### 🎨 Адаптивная админ-панель
- **Десктоп**: Ant Design `<Table>` с сортируемыми колонками, фиксированными заголовками
- **Мобильные**: `<List>` с `<Card>` компонентами для удобного touch-взаимодействия
- **Автоматическое переключение макета** в зависимости от размера экрана (breakpoint: `md` = 768px)

### ♾️ Бесконечная прокрутка
- **Cursor-based загрузка** - быстрая производительность на больших датасетах
- **Intersection Observer API** - триггерит `fetchNextPage()` когда нижний элемент попадает во viewport
- **Состояния загрузки** - skeleton screens и спиннеры для лучшего UX
- **Определение конца** - индикатор "Все машины загружены" когда данных больше нет

### 🔍 Серверный поиск
- **Debounced ввод** - задержка 500мс чтобы избежать избыточных API вызовов
- **SQL ILIKE запросы** - поиск по колонкам `brand`, `model` и `color`
- **Счетчик результатов в реальном времени** - "Найдено 1,234 результатов"
- **С учетом пагинации** - поиск сохраняет позицию курсора

### 🤖 Telegram Bot с LLM
- **Обработка естественного языка** - "Покажи красные Тойоты дешевле ¥2М  с 2020 года"
- **OpenAI GPT-4o-mini** - извлекает структурированные фильтры из запросов пользователя
- **Динамическая генерация SQL** - конвертирует JSON фильтры в SQLAlchemy запросы
- **Inline результаты** - отправляет объявления с кликабельными ссылками

### 🔒 Безопасная аутентификация
- **JWT токены** - stateless аутентификация с expiry 60 минут
- **bcrypt хеширование паролей** - индустриальный стандарт с 12 раундами
- **HTTP-only cookies** - защита от XSS атак (настраиваемо)
- **Защищенные роуты** - неавторизованный доступ возвращает 401

### 🐳 Production-ready Docker
- **Multi-stage сборки** - финальные образы на 80% меньше
- **Non-root пользователи** - все сервисы работают без привилегий
- **Health checks** - автоматический перезапуск контейнеров при сбое
- **Автоматические миграции** - `entrypoint.sh` запускает Alembic при старте
- **Деплой одной командой** - `docker-compose up -d`

---

## 🛠 Технологический стек

### Backend
| Технология | Версия | Назначение |
|------------|--------|------------|
| **Python** | 3.11 | Runtime окружение |
| **FastAPI** | 0.100+ | Асинхронный REST API фреймворк |
| **SQLAlchemy** | 2.0 | ORM с поддержкой async |
| **Alembic** | 1.12+ | Миграции базы данных |
| **asyncpg** | 0.28+ | PostgreSQL async драйвер |
| **Pydantic** | 2.0+ | Валидация данных |
| **python-jose** | 3.3+ | Генерация JWT токенов |
| **bcrypt** | 4.1.2 | Хеширование паролей |

### Frontend
| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 19 | UI фреймворк |
| **TypeScript** | 5.9 | Типобезопасность |
| **Vite** | 7.3 | Сборщик и dev сервер |
| **Ant Design** | 6.3+ | Библиотека компонентов |
| **TanStack Query** | 5.90+ | Управление серверным состоянием |
| **Zustand** | 4.5+ | Клиентское состояние (auth) |
| **Axios** | 1.6+ | HTTP клиент |
| **Tailwind CSS** | 3.4+ | Utility-first CSS |

### Scraper
| Технология | Версия | Назначение |
|------------|--------|------------|
| **httpx** | 0.27+ | Асинхронные HTTP запросы |
| **BeautifulSoup4** | 4.12+ | Парсинг HTML |
| **SQLAlchemy** | 2.0 | Персистентность данных |

### Telegram Bot
| Технология | Версия | Назначение |
|------------|--------|------------|
| **aiogram** | 3.4+ | Telegram Bot API |
| **OpenAI SDK** | 1.14+ | Интеграция с LLM (GPT-4) |
| **SQLAlchemy** | 2.0 | Запросы к БД |

### Инфраструктура
| Технология | Версия | Назначение |
|------------|--------|------------|
| **PostgreSQL** | 15-alpine | Основная БД |
| **Docker** | 20.10+ | Контейнеризация |
| **Docker Compose** | 2.0+ | Оркестрация контейнеров |
| **Nginx** | stable-alpine | Веб-сервер для frontend |

---

## 📊 Структура проекта

```
auto-assistent-llm/
├── backend/                  # FastAPI приложение
│   ├── alembic/              # Миграции БД
│   ├── auth.py               # JWT и bcrypt утилиты
│   ├── database.py           # SQLAlchemy async engine
│   ├── main.py               # FastAPI app и роуты
│   ├── models.py             # ORM модели (User, Car)
│   ├── schemas.py            # Pydantic схемы
│   ├── seed.py               # Seeder для admin пользователя
│   ├── entrypoint.sh         # Docker init скрипт
│   ├── Dockerfile            # Multi-stage Python сборка
│   └── requirements.txt      # Python зависимости
│
├── frontend/                 # React SPA
│   ├── src/
│   │   ├── api/              # Axios клиент
│   │   ├── components/       # React компоненты
│   │   │   ├── CarsTable.tsx # Основная таблица/список
│   │   │   └── ui/           # Переиспользуемые UI компоненты
│   │   ├── features/
│   │   │   └── auth/         # Форма логина
│   │   ├── hooks/            # Кастомные хуки (useCars)
│   │   ├── store/            # Zustand store
│   │   ├── types/            # TypeScript интерфейсы
│   │   └── App.tsx           # Корневой компонент
│   ├── nginx.conf            # Production Nginx конфиг
│   ├── Dockerfile            # Multi-stage Node + Nginx
│   └── package.json          # NPM зависимости
│
├── scraper/                  # Web scraper воркер
│   ├── main.py               # httpx + BeautifulSoup4
│   ├── database.py           # Общее подключение к БД
│   ├── models.py             # Общие ORM модели
│   ├── Dockerfile            # Python slim сборка
│   └── requirements.txt      # Зависимости scraper
│
├── bot/                      # Telegram бот
│   ├── main.py               # aiogram + OpenAI
│   ├── database.py           # Общее подключение к БД
│   ├── models.py             # Общие ORM модели
│   ├── Dockerfile            # Python slim сборка
│   └── requirements.txt      # Зависимости бота
│
├── docker-compose.yml        # Конфиг оркестрации
├── .env.example              # Шаблон окружения
├── DOCKER.md                 # Руководство по деплою
└── README.md                 # Этот файл
```

---

## 📝 Разработка

### Локальная разработка (без Docker)

**Требования:**
- Установите [uv](https://docs.astral.sh/uv/getting-started/installation/) - быстрый менеджер пакетов Python
- Установите [PostgreSQL 15+](https://www.postgresql.org/download/) и создайте базу данных

**Настройка:**

1. **Создайте базу данных PostgreSQL:**
```bash
psql -U postgres
CREATE DATABASE auto_assistent;
CREATE USER auto_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE auto_assistent TO auto_user;
\q
```

2. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env и измените:
# DATABASE_URL=postgresql+asyncpg://auto_user:your_password@localhost:5432/auto_assistent
# (используйте 'localhost' вместо 'db' для локальной разработки)
```

3. **Запустите сервисы:**

```bash
# Backend
cd backend
uv venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
uv pip install -r requirements.txt
alembic upgrade head
python seed.py
uvicorn main:app --reload

# Frontend (в отдельном терминале)
cd frontend
npm install
npm run dev

# Scraper (в отдельном терминале)
cd scraper
uv venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
uv pip install -r requirements.txt
python main.py

# Bot (в отдельном терминале)
cd bot
uv venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
uv pip install -r requirements.txt
python main.py
```

**Локальные миграции базы данных:**
```bash
cd backend
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# Создать новую миграцию
alembic revision --autogenerate -m "описание"

# Применить миграции
alembic upgrade head

# Откатить одну миграцию назад
alembic downgrade -1
```

---

### Разработка с Docker

**Миграции базы данных:**

```bash
# Создать новую миграцию
docker-compose exec backend alembic revision --autogenerate -m "описание"

# Применить миграции
docker-compose exec backend alembic upgrade head

# Откатить одну миграцию назад
docker-compose exec backend alembic downgrade -1
```

**Логи и отладка:**

```bash
# Просмотр всех логов
docker-compose logs -f

# Просмотр конкретного сервиса
docker-compose logs -f backend

# Доступ к shell контейнера
docker-compose exec backend sh
```

---

## 📄 Лицензия

Этот проект лицензирован по лицензии MIT - см. файл [LICENSE](LICENSE) для деталей.

---

## 🙏 Благодарности

- [carsensor.net](https://www.carsensor.net/) - Источник данных
- [FastAPI](https://fastapi.tiangolo.com/) - Современный Python фреймворк
- [Ant Design](https://ant.design/) - Библиотека React компонентов
- [TanStack Query](https://tanstack.com/query) - Мощная синхронизация данных

---

## 🔒 Безопасность

**Важные практики безопасности:**

- **Никогда не коммитьте файл `.env`** в систему контроля версий - он содержит конфиденциальные данные
- Всегда используйте предоставленный `.env.example` в качестве шаблона
- Генерируйте сильные пароли и секреты для production развертывания
- Регулярно меняйте JWT секреты и API ключи
- Используйте конфигурации специфичные для окружения: разработка, staging, production

**Включено в `.gitignore`:**
```
.env
*.log
__pycache__/
*.pyc
node_modules/
```

---

## ⚠️ Отказ от ответственности

**Этот проект предоставляется исключительно в образовательных и демонстрационных целях.**

Используя это программное обеспечение, вы соглашаетесь со следующим:

1. **Образовательная цель**: Этот проект является технической демонстрацией современных практик веб-разработки, микросервисной архитектуры и методов обработки данных.

2. **Соблюдение правил веб-скрапинга**: Компонент веб-скрапера предназначен для работы с публично доступными данными. Пользователи **обязаны**:
   - Ознакомиться и соблюдать файл `robots.txt` целевого веб-сайта
   - Соблюдать Условия использования (ToS) веб-сайта
   - Реализовать соответствующее ограничение скорости, чтобы избежать перегрузки серверов
   - Получить явное разрешение от владельцев сайта, когда это требуется

3. **Без гарантий**: Это программное обеспечение предоставляется "как есть" без каких-либо гарантий. См. файл [LICENSE](LICENSE) для подробностей.

4. **Ответственность пользователя**: Пользователи несут единоличную ответственность за обеспечение того, что их использование данного программного обеспечения соответствует всем применимым законам, нормативным актам и политикам веб-сайтов в их юрисдикции.

5. **Использование данных**: Любые собранные данные должны использоваться этично и в соответствии с применимыми нормами защиты данных (GDPR, CCPA и т.д.).

**Автор и участники не несут ответственности за любое неправомерное использование данного программного обеспечения.**

